apiVersion: apps/v1
kind: Deployment
metadata:
  name: guild-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guild
  template:
    metadata:
      labels:
        app: guild
    spec:
      containers:
        - name: guild
          image: 425409/guildservice:main
